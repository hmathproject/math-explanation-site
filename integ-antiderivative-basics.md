---
layout: default
title: 不定積分とは何か — 微分の逆から基本公式へ
permalink: /integ-antiderivative-basics/
description: "原始関数の定義と不定積分の基本公式 ∫xⁿ dx=xⁿ⁺¹/(n+1)+C を解説。(xⁿ)'=nxⁿ⁻¹ を逆読みするだけで公式が得られる理由、積分定数 C が曲線の族を表す意味を確認する（高校数II）。"
---

[サイトトップ](/) / [積分](/integral/) / [不定積分](/integ-indefinite/)

---

# 不定積分とは何か — 微分の逆から基本公式へ

---

## 動機: 「微分の逆演算」としての積分

微分は「関数 \\( F(x) \\) を与えたとき、その変化の速さ \\( F'(x) \\) を求める」演算でした。積分はその逆で、「変化の速さ \\( f(x) \\) が分かっているとき、もとの関数を復元する」演算です。

たとえば \\( f(x) = 2x \\) という速さで変化しているとき、\\( F'(x) = 2x \\) を満たす \\( F(x) \\) は何でしょうか。\\( F(x) = x^2 \\) が一つの答えですが、\\( F(x) = x^2 + 3 \\) も \\( F(x) = x^2 - 7 \\) も同じ微分結果を持ちます。これが、不定積分が「一つの関数」ではなく「定数の差だけ異なる関数の族」を返す理由です。

---

## 原始関数の定義

<div>
$$
F'(x) = f(x)
$$
</div>

を満たす \\( F(x) \\) を \\( f(x) \\) の**原始関数**と言います。\\( F(x) \\) が原始関数ならば、任意の定数 \\( C \\) に対して \\( F(x) + C \\) も原始関数です。これは

<div>
$$
\frac{d}{dx}(F(x) + C) = F'(x) + 0 = f(x)
$$
</div>

となるからです。逆に、\\( f(x) \\) の原始関数はすべて \\( F(x) + C \\) の形で表せることが証明されます（2 つの原始関数の差は定数）。

**不定積分**はすべての原始関数をまとめて

<div>
$$
\int f(x)\,dx = F(x) + C
$$
</div>

と表したものです。\\( C \\) を**積分定数**と言います。

---

## 基本公式の導出

微分公式 \\( (x^n)' = nx^{n-1} \\) を「逆読み」します。\\( nx^{n-1} \\) を積分すると \\( x^n \\) に戻るはずですから、添字を \\( n \to n+1 \\) にずらすと

<div>
$$
\int x^n\,dx = \frac{x^{n+1}}{n+1} + C \qquad (n \neq -1)
$$
</div>

が得られます。確かめ: \\( \left(\frac{x^{n+1}}{n+1}\right)' = \frac{(n+1)x^n}{n+1} = x^n \\) ✓

定数の積分については \\( (kx)' = k \\) を逆読みして

<div>
$$
\int k\,dx = kx + C
$$
</div>

| 元の関数 \\( f(x) \\) | 不定積分 \\( \int f(x)\,dx \\) |
|---|---|
| \\( x^n \; (n \neq -1) \\) | \\( \frac{x^{n+1}}{n+1} + C \\) |
| \\( x^2 \\) | \\( \frac{x^3}{3} + C \\) |
| \\( x \\) | \\( \frac{x^2}{2} + C \\) |
| \\( 1 \\) | \\( x + C \\) |
| 定数 \\( k \\) | \\( kx + C \\) |

---

## 計算例: \\( \int(3x^2 + 2x - 1)\,dx \\)

**Step 1:** 各項に公式を適用する。

<div>
$$
\int 3x^2\,dx = 3 \cdot \frac{x^3}{3} = x^3
$$
</div>

<div>
$$
\int 2x\,dx = 2 \cdot \frac{x^2}{2} = x^2
$$
</div>

<div>
$$
\int (-1)\,dx = -x
$$
</div>

**Step 2:** 合算して積分定数を付ける。

<div>
$$
\int(3x^2 + 2x - 1)\,dx = x^3 + x^2 - x + C
$$
</div>

**Step 3:** 微分して確かめる。\\( (x^3 + x^2 - x + C)' = 3x^2 + 2x - 1 \\) ✓

---

## 積分定数 \\( C \\) の意味

\\( \int x^2\,dx = \frac{x^3}{3} + C \\) において、\\( C \\) の値によって異なる曲線が得られます。これらはすべて「微分すると \\( x^2 \\) になる曲線」であり、縦方向に平行移動した**曲線の族**を形成します。

グラフで見ると、\\( C = 0, 1, 2, -1 \\) などの場合にそれぞれ \\( y = \frac{x^3}{3} \\), \\( y = \frac{x^3}{3} + 1 \\), ... という曲線の族が描かれます。どの曲線も形は同じで、縦方向のずれが \\( C \\) の差に対応しています。初期条件（特定の点を通るという条件）を与えることで、この族の中から一本の曲線が確定します（次の記事で扱います）。

![微分と積分の逆演算の関係（左）/ ∫x²dx=x³/3+C の族グラフ（右）](/assets/images/integ-antiderivative-basics-combined.png)

---

## もっと練習したい方へ

不定積分の基本公式・線形性・積分定数の決定を扱った解説 PDF を無料で配布しています。

<a class="pdf-btn" href="/assets/pdf/integ-indefinite-pack.pdf">PDFをダウンロードする（無料）</a>

---

[不定積分](/integ-indefinite/)　／　→ [不定積分の計算](/integ-antiderivative-calc/)
